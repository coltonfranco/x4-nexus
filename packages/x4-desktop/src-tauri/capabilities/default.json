{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: core APIs + native folder picker. Applies to the local asset origin (loader) and the local server origin the webview redirects to.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:8765", "http://localhost:8765"]
  },
  "permissions": [
    "core:default",
    "dialog:default",
    "updater:default",
    "process:default"
  ]
}
